# PHP2xAI
AI framework with a PHP frontend and a C++ runtime for training and inference of custom neural networks. PHP manages datasets, computational graph configuration, and training orchestration, while the C++ runtime executes high-performance numerical computations.
